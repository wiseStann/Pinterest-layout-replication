'use strict'

export default class App {
    #root
    #pins

    constructor(root, pins) {
        this.#root = root;
        this.#pins = pins;
    }

    renderLayoutPage(route) {
        this.#root.InnerH
    }
};
